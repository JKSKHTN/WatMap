import React from 'react'
import { Router, Link } from "@reach/router"
import { Button } from "react-bootstrap";

export default function LogOut() {
    const {logout} f

	return (
		<div>
            <Button onClick={} variant="primary">Logout</Button> 
		</div>
	)
}